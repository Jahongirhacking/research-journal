export const enum SearchParams {
  Drawer = "drawer",
  Step = "step",
}

export const enum DrawerChildTypes {
  RequiredContact = "required-contact",
  WorkplaceList = "workplace-list",
}

export enum FilterProps {
  ArticleName = "filter[article-name]",
  AuthorName = "filter[author-name]",
  CountryName = "filter[country-name]",
  ResourceLanguage = "filter[resource-language]",
  ResourceField = "filter[resource-field]",
  ArticleDate = "filter[article-date]",
}

export enum SortProps {
  Type = "sort[type]",
  Order = "sort[order]",
}
